export interface IUser {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    lastLoginDate: string;
    nickname?: string;
    createdDate: string;
    updatedDate: string;
}
